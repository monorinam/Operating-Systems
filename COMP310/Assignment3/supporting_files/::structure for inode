//structure for inode
//structure for superblock
//structure for data blocks
//struct for inode table (array of inodes, and if they are open or not? this can go in fildes no need here)
//struct for directories (or array)
// file descriptor table
typedef struct filedes{
	int rd_wr_ptr;
	int inode;
	int open;
}